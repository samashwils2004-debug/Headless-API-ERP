export default function TemplatesPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-semibold">Templates</h2>
      <div className="rounded-md border border-[var(--border-default)] bg-[var(--bg-secondary)] p-4 text-sm text-[var(--text-secondary)]">
        Template blueprints are reusable infrastructure seeds: workflows, roles, schemas, and event definitions with
        versioned deployment flows.
      </div>
    </div>
  );
}
